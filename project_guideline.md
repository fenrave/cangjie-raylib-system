<sub>Mostly as a reminder to myself</sub>

> 1. Use `this` keyword inside objects to avoid ambiguity & to avoid scoping issues.
> 2. Use `interface` objects whenever possible for Raylib functions, as to simplify usage in other paradigm schemes.
> 3. Attempt to rewrite/de-abstract components of Raylib into native Cangjie components.
> 4. Always use an explicit return unless return type is `Unit`.